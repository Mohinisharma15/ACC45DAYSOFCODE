
// sum of Natural number using recursion 

#include <stdio.h>

int rec(int n){
    if(n<=1){
        return n;
    }
    else{
        return n+rec(n-1);
    }
}

int main()
{
    
int n;
printf("Enter the number \n");
scanf("%d",&n);

printf("%d",rec(n));
    

    return 0;
}
